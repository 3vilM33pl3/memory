[Unit]
Description=Memory Layer Service
After=network.target postgresql.service

[Service]
Type=simple
User=memory-layer
Group=memory-layer
EnvironmentFile=/etc/memory-layer/memory-layer.env
WorkingDirectory=/var/lib/memory-layer
ExecStart=/usr/bin/memory --config /etc/memory-layer/memory-layer.toml service run
Restart=on-failure
RestartSec=2
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/var/lib/memory-layer /var/log/memory-layer /etc/memory-layer

[Install]
WantedBy=multi-user.target
